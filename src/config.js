// === App Configuration ===
// 이 파일의 값들을 수정하여 당신의 웹 애플리케이션을 커스터마이징하세요.
// 코드의 다른 부분은 수정할 필요가 없습니다.

export const APP_CONFIG = {
  // --- 일반 설정 ---
  GENERAL: {
    SITE_TITLE: "Prompt Vault",
    FAVICON_URL: "data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>⚡</text></svg>",
  },

  // --- UI 텍스트 설정 ---
  UI_TEXTS: {
    MAIN_TITLE: "Prompt Vault",
    CAPTURE_PLACEHOLDER: "번뜩이는 아이디어를 여기에 기록하세요... (Ctrl+Enter로 저장)",
    SORT_MODE_TITLE: "정리할 프롬프트",
    SORT_MODE_PROMPT: "AI가 추천하는 카테고리로 프롬프트를 정리하세요.",
    EMPTY_PROMPTS_MESSAGE: "아직 프롬프트가 없습니다. 첫 아이디어를 기록해보세요!",
    EMPTY_SORT_MODE_MESSAGE: "정리할 프롬프트가 없습니다! ✨",
    COMMAND_PALETTE_PLACEHOLDER: "명령어 검색...",
    SORT_MODE_COMPLETE_MESSAGE: "✨정리 완료",
  },

  // --- AI 서비스 설정 ---
  AI_SERVICE: {
    // [완료] CEO님의 API 키를 여기에 직접 반영했습니다.
    API_KEY: "AIzaSyDSIRzDsbonDZwuDB6RRmYYy-vR2Cqupmg", 
    
    // API 호출에 사용할 모델 이름
    MODEL_NAME: "gemini-2.5-pro",

    // [수정] AI가 제목, 요약, 초안을 JSON 형식으로 반환하도록 프롬프트 템플릿을 업데이트합니다.
    STRATEGIST_AI_PROMPT_TEMPLATE: `
### 🎯 최종 목표
사용자의 원본 아이디어를 '전략 프롬프트'로 재창조하고, 해당 프롬프트의 핵심을 담은 '추천 제목'과 '핵심 요약'을 함께 생성하여 완벽한 JSON 객체로 반환한다.

### 👤 역할 부여
너는 세계 최고의 프롬프트 엔지니어이자, 사용자의 생각을 한눈에 파악하여 핵심만 추출하는 AI 전략 컨설턴트다.

### 📝 단계별 실행 지침
1.  **아이디어 분석:** 아래의 '사용자 원본 아이디어'에서 핵심 목표, 원하는 결과물, 숨겨진 의도를 완벽하게 파악한다.
2.  **전략 프롬프트 생성:** 파악한 내용을 바탕으로, AI의 성능을 극한으로 끌어낼 수 있도록 '역할 부여', '상황(Context)', '명확한 지시사항', '결과물 형식', '제약 조건' 등을 포함한 구조화된 '전략 프롬프트(draft)'를 작성한다. 사용자가 "이렇게까지 생각 못했는데?" 라고 반응할 만한 깊이를 더해야 한다.
3.  **핵심 추출:**
    * **추천 제목 (title):** 생성된 '전략 프롬프트'의 목적과 핵심 가치를 20자 내외의 간결하고 명확한 제목으로 만든다.
    * **핵심 요약 (summary):** 전체 프롬프트의 내용을 읽지 않아도, 이 프롬프트가 무엇을 위한 것인지 150자 내외로 완벽하게 이해할 수 있도록 요약한다.
4.  **JSON 형식으로 출력:** 아래 '출력 형식'을 **반드시** 준수하여, 어떠한 추가 설명이나 서론 없이 오직 JSON 객체만을 반환한다.

### 🚫 절대 금지사항
-   단순히 문장을 다듬거나 원본 아이디어를 요약해서는 안 된다. (draft)
-   추상적이거나 일반적인 표현 대신, 구체적이고 실행 가능한 지시를 사용한다. (draft)
-   JSON 형식이나 키 이름을 절대로 변경해서는 안 된다.

---
### 💡 사용자 원본 아이디어
{userInput}
---

### 📦 출력 형식 (JSON)
\`\`\`json
{
  "title": "AI가 생성한 추천 제목",
  "summary": "AI가 생성한 150자 내외의 핵심 요약",
  "draft": "재창조된 전체 전략 프롬프트 내용..."
}
\`\`\`
`,
    // [신규] 카테고리 추천을 위한 AI 프롬프트 템플릿
    CATEGORY_SUGGESTION_PROMPT_TEMPLATE: `
### 🎯 분석 목표
사용자의 프롬프트 내용을 분석하여, 제시된 카테고리 목록 중에서 가장 적합한 **상위 2개**를 추천한다.

### 👤 역할 부여
너는 사용자의 생각을 완벽하게 이해하고 정리하는 AI 비서다.

### 📚 분석 대상
- **사용자 프롬프트:** {userInput}
- **카테고리 목록:** {categoryList}

### 📝 출력 형식 (JSON)
- 반드시 다음의 JSON 형식과 키 이름을 준수해야 한다.
- 가장 확률이 높은 추천 카테고리를 'best'에, 두 번째 추천을 'second'에 할당한다.
- 절대로 다른 설명 없이 JSON 객체만 반환해야 한다.

\`\`\`json
{
  "best": "추천 카테고리 1",
  "second": "추천 카테고리 2"
}
\`\`\`
`,
    // [신규] 자동 구조화를 위한 AI 프롬프트 템플릿
    AUTO_FORMATTER_PROMPT_TEMPLATE: `
### 🎯 최종 목표
사용자가 자유롭게 입력한 원본 텍스트의 의미적 구조(목표, 역할, 맥락, 제약 조건 등)를 파악하여, 정보의 손실이나 변경 없이 가독성 높은 마크다운 형식으로 재구성한다.

### 👤 역할 부여
너는 사용자의 생각을 왜곡 없이 가장 명확하게 구조화하는 AI 문서 편집 전문가다.

### 📝 단계별 실행 지침
1.  **의미 분석:** 아래 '사용자 원본 텍스트'를 분석하여 '목표', '역할', '결과물 형식', '제약 조건' 등 핵심 의미 단위를 식별한다. 사용자가 '# 목표', '## Goal:', '- 역할:' 등 어떤 형식을 사용했더라도 그 의도를 파악해야 한다.
2.  **마크다운 재구성:** 식별된 의미 단위들을 일관된 마크다운 형식(예: '## 목표', '## 역할', '- 항목')으로 변환한다. 원본의 텍스트 내용은 단 한 글자도 수정, 추가, 삭제해서는 안 된다. 오직 구조만 변경한다.
3.  **순서 유지:** 원본 텍스트에 나타난 정보의 순서를 최대한 유지하여 재구성한다.
4.  **순수 텍스트 출력:** 어떠한 추가 설명, 인사, 서론 없이 오직 변환된 마크다운 텍스트만을 반환한다.

### 🚫 절대 금지사항
-   원본 텍스트의 내용을 요약하거나, 문장을 바꾸거나, 새로운 내용을 추가해서는 안 된다.
-   마크다운 형식 외에 다른 텍스트를 포함해서는 안 된다.

---
### 💡 사용자 원본 텍스트
{userInput}
---
`
  },

  // --- 기능 플래그 ---
  FEATURES: {
    ENABLE_MARKDOWN_PARSING: true,
    ENABLE_SYNTAX_HIGHLIGHTING: true,
    ENABLE_SORT_MODE_NOTIFICATIONS: true,
  },

  // --- 테마 및 스타일 설정 ---
  THEME: {
    DEFAULT_THEME: 'dark',
    PRIMARY_COLOR: "#4F46E5", // Indigo
    FONT_FAMILY: "'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif",
  },
};